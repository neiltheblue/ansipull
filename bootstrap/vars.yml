# The main user to create in the new system.
new_user: neil

# The hostname. 
# This can be changed after installation by editing /etc/hostname.  
hostname: box

# The locale to use of the system.
locale: en_GB

# The console keymap to use.
keymap: uk

# reboot after finish
reboot: false
