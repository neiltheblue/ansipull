- name: Configure DHCPC
  hosts: all
  
  tasks:

   - name: pacstrap
     ansible.builtin.shell: |
       yes | pacstrap -K /mnt dhcpcd

   - name: Start service
     ansible.builtin.shell: |
       arch-chroot /mnt \
        systemctl enable dhcpcd.service

       
